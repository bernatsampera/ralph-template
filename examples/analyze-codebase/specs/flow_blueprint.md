{PROJECT_NAME}
{UNDERLINE}

STACK: {tech} | {tech} | {tech} | ...

---

## OVERALL FLOW

```
┌─────────────────────┐
│      {Step 1}       │
└──────────┬──────────┘
            │
            ▼
┌─────────────────────┐
│      {Step 2}       │
└──────────┬──────────┘
            │
            ▼
┌─────────────────────┐
│      {Step 3}       │
└──────────┬──────────┘
            │
            ▼
┌─────────────────────┐
│      {Step 4}       │
└──────────┬──────────┘
            │
            ▼
┌─────────────────────┐
│      {Step 5}       │
└─────────────────────┘
```

{Brief description of what the application does and how data flows through these steps.}

**IMPORTANT:** Each step name must be a single concept that includes a specific technology, framework, library, or component name (e.g., "NestJS Server Init" instead of "Server Init").

---

## FLOW DETAILS

{Each section below corresponds to a step in the flow above. Section headers must match the step names from the diagram above. Describe the purpose, key files, and structure for each step.}

### {Step 1}

**Purpose:** {What this step does}

{Brief context about this step's role in the overall flow.}

**Key Files:**
{List the main files involved in this step.}
- `{path}` → {role}

**Structure:**
{Describe the directory organization for this step.}
- `{dir}/` → {purpose}

---

### {Step 2}

**Purpose:** {What this step does}

{Brief context about this step's role in the overall flow.}

**Key Files:**
- `{path}` → {role}

**APIs:**
{Optional: List external or internal APIs used.}
- `{api}` → {usage}

**Structure:**
- `{dir}/` → {purpose}

---

### {Step 3}

**Purpose:** {What this step does}

{Brief context about this step's role in the overall flow.}

**Key Files:**
- `{path}` → {role}

**Structure:**
- `{dir}/` → {purpose}

---

### {Step 4}

**Purpose:** {What this step does}

{Brief context about this step's role in the overall flow.}

**Key Files:**
- `{path}` → {role}

**APIs:**
{Optional: List external or internal APIs used.}
- `{api}` → {usage}

**Structure:**
- `{dir}/` → {purpose}

---

### {Step 5}

**Purpose:** {What this step does}

{Brief context about this step's role in the overall flow.}

**Key Files:**
- `{path}` → {role}

**Structure:**
- `{dir}/` → {purpose}

---

## COMMANDS

{List common commands for working with this project.}

- `{command}` → {description}
